{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Section list</h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Section</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('section_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.section }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-default" href="{{ path('section_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-default" href="{{ path('section_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            <ul>
            <li>
            <a class="btn btn-default" href="{{ path('section_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
