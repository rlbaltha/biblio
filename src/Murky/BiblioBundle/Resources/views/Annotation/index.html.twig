{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Annotation list</h1>

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Annotation</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('annotation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.annotation }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-default" href="{{ path('annotation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-default" href="{{ path('annotation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

            <ul>
            <li>
            <a class="btn btn-default" href="{{ path('annotation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
